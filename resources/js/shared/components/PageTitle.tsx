const PageTitle = ({ title }: { title: string }) => {
    return <h1 className="rounded border-r-5 w-fit  border-r-mainColor ps-4 text-xl lg:text-3xl font-bold">{title}</h1>;
};

export default PageTitle;
